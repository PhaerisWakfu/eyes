{
  "name": "eyes",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "fetch": "tsx scripts/fetch.ts",
    "wechat": "tsx scripts/wechat-export.ts",
    "dev": "npm run copy-data && astro dev",
    "copy-data": "tsx scripts/copy-data.ts",
    "download-images": "tsx scripts/download-images.ts",
    "download-images:verbose": "tsx scripts/download-images.ts --verbose",
    "build": "npm run copy-data && astro build",
    "postbuild": "tsx scripts/cf-assetsignore.ts",
    "preview": "astro preview",
    "preview:cf": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^5.5.0",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "@astrojs/cloudflare": "^12.6.13",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",
    "wrangler": "^4.72.0"
  }
}
